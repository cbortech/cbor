{
  "name": "cbor-site",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "clean": "node -e \"require('fs').rmSync('dist',{recursive:true,force:true})\"",
    "dev": "vite",
    "build": "vite build",
    "typecheck": "tsc --noEmit",
    "preview": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "@cbortech/cbor": "file:..",
    "@cbortech/hash-extension": "^0.2.2",
    "@cbortech/uuid-extension": "^0.3.2",
    "@codemirror/commands": "^6.10.4",
    "@codemirror/lint": "^6.9.7",
    "@codemirror/state": "^6.7.0",
    "@codemirror/view": "^6.43.3"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "vite": "^8.1.0",
    "wrangler": "^4.105.0"
  }
}
